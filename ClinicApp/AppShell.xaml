<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="ClinicApp.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:ClinicApp"
    Shell.FlyoutBehavior="Disabled">

    <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />

    <TabBar>
        <Tab x:Name="HomeTab">
            <!--<ShellContent ContentTemplate="{DataTemplate views:CitasPage}" Route="home"/>-->
        </Tab>
        <Tab x:Name="SectionsTab">
            <!--<ShellContent ContentTemplate="{DataTemplate views:CitasPage}" Route="sections"/>-->
        </Tab>
        <Tab x:Name="BookmarksTab">
            <!--<ShellContent ContentTemplate="{DataTemplate views:RegisterPage}" Route="bookmarks"/>-->
        </Tab>
        <Tab x:Name="ProfileTab">
            <!--<ShellContent ContentTemplate="{DataTemplate views:SettingPage}" Route="profile"/>-->
        </Tab>
    </TabBar>

</Shell>
